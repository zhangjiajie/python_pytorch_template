name: CI
on: workflow_dispatch
jobs: 
    setup-env:
        runs-on: ubuntu-latest
        steps:
            - name: pull git repo 
              uses: actions/checkout@v4
            - name: print greeting
              run: echo "Hello"